name: 02 - Eventos
on:   
  pull_request:
  schedule:
    - cron: '21 * * * *'
  workflow_dispatch: 

jobs:
  echo: 
    runs-on: ubuntu-latest
    steps:
      - name: ShowTrigger
        run: "Event name: ${{ github.event_name }}"    